# -*- coding: UTF-8 -*-
"""
功能开关配置文件
该文件用于控制系统各个功能模块的启用/禁用状态。
通过修改此文件，可以灵活地开启或关闭特定功能，便于调试和功能管理。
"""

# =========================================================
# 功能开关配置
# =========================================================
FEATURE_CONFIG = {
    # 是否启用 Dify 知识库集成
    # True: 在生成用例时会调用 Dify API 检索相关知识
    # False: 仅使用 LLM 自身的知识进行生成
    "use_knowledge": True,
    
    # 是否启用 LLM 模型
    # True: 使用 LLM 进行智能生成
    # False: 可能用于测试或降级模式（具体行为取决于业务逻辑）
    "use_llm": True,
    
    # 是否启用测试维度分析
    # True: 在生成用例前，先分析测试维度（如功能、性能、安全等），提高覆盖率
    # False: 直接生成用例，不进行维度分析
    "use_test_dimension": True,
    
    # 是否启用上下文管理器
    # True: 维护对话上下文，支持多轮对话和记忆
    # False: 每次请求都是独立的，无上下文记忆
    "use_context_manager": True
}
